<Window x:Class="MasterSlavesSync.Views.NewWorkspaceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MasterSlavesSync.Views"
        xmlns:controls="clr-namespace:MasterSlavesSync.Controls"
        mc:Ignorable="d"
        Title="New Workspace"
        Height="360"
        Width="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">

    <Border Style="{StaticResource LayoutParent_Border}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="85"/>
                <RowDefinition/>
                <RowDefinition Height="85"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Style="{StaticResource LayoutChild_Border}">

                <controls:TextInput Title="Ruta principal" Text=""/>

            </Border>

            <Border Grid.Row="1"
                    Style="{StaticResource LayoutChild_Border}">

            </Border>

            <Border Grid.Row="2"
                    Style="{StaticResource LayoutChild_Border}">
                
            </Border>

        </Grid>
    </Border>
    
</Window>
